'use strict';

/**
 * artwork-archive controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::artwork-archive.artwork-archive');
